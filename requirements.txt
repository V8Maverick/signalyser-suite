# Signalyser Suite — shared core dependencies.
# Tool-specific deps (beautifulsoup4, youtube-transcript-api, matplotlib, ...)
# are added per phase as each tool lands.
requests>=2.33.1
python-dotenv>=1.2.2
anthropic>=0.40.0

# Web front end (signalyser_web): FastAPI/uvicorn server, Jinja templates,
# form parsing, and server-side markdown rendering for the report viewer.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
markdown>=3.6
